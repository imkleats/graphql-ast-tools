describe('Example rules', () => {
    // TBD: What standard rules actually make sense to include?
    // Any default rules should have a test, and this might demonstrate
    // how TranslationRules can be tested in general.
    test.todo('should generate a visitor function');
})